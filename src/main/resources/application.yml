tgbot:
  botToken: telegram_bot_token
  chatId: telegram_chat_id

log:
  events:
    include: "error"
    exclude:

container:
  events:
    exclude:
    include:
  names:
    include:
    exclude:


